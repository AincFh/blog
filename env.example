# ============================================
# 环境变量配置
# ============================================

# JWT 密钥 - 用于签发和验证 Token
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-please-change-this-min-32-characters-long

# Cloudflare Account ID (从 Cloudflare 控制台获取)
# https://dash.cloudflare.com/ -> 右侧边栏
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# 环境
NODE_ENV=development

# 开发环境管理员账号（仅本地开发）
# ⚠️ 生产环境必须删除或设为空
NEXT_PUBLIC_DEV_ADMIN_EMAIL=admin@example.com
NEXT_PUBLIC_DEV_ADMIN_PASSWORD=admin123

# API 配置
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ============================================
# 可选: AI 功能配置
# ============================================
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# ============================================
# 可选: 邮件服务 (SMTP)
# ============================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=博客系统

# ============================================
# 可选: 第三方服务
# ============================================
GOOGLE_ANALYTICS_ID=
BAIDU_TONGJI_ID=

# ============================================
# 生产环境配置（部署时启用）
# ============================================

# 生产 API URL
# NEXT_PUBLIC_API_URL=https://your-domain.com/api

# 注意：
# 1. 复制此文件为 .env.local 并填入真实值
# 2. .env.local 已在 .gitignore 中，不会被提交
# 3. JWT_SECRET 至少 32 个字符，建议使用随机字符串
# 4. 生产环境部署时，需在 Cloudflare Pages 设置中配置环境变量
