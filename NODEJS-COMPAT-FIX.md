# 🔧 添加 Node.js 兼容性标志

## 快速操作指南

您的网站现在显示兼容性错误，需要在 Cloudflare Pages 设置中添加 `nodejs_compat` 标志。

### 第 1 步：进入项目设置

1. 访问 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 点击 **Workers & Pages**
3. 找到您的项目（blog 或 blog-web）
4. 点击 **Settings** 标签

### 第 2 步：添加兼容性标志

1. 滚动到 **Functions** 部分
2. 找到 **Compatibility flags** 选项
3. 点击 **Add flag**

### 第 3 步：配置标志

**Production 环境**：
- 在输入框中输入：`nodejs_compat`
- 点击 **Save**

**Preview 环境**：
- 重复上述步骤
- 在 Preview 环境也添加 `nodejs_compat`
- 点击 **Save**

### 第 4 步：重新部署

配置完成后：
- 返回 **Deployments** 标签
- 点击最新部署的 **Retry deployment**
- 或者等待几分钟让系统自动检测最新推送

---

## ✅ 完成！

添加标志后，网站应该能正常访问了。

**访问测试**：
- 前台首页：`https://your-domain.pages.dev`
- 管理后台：`https://your-domain.pages.dev/admin/login`

---

## 📝 技术说明

**为什么需要 nodejs_compat？**

Next.js 使用了一些 Node.js API（如 crypto、buffer 等），这个标志启用了 Cloudflare Workers 对这些 API 的兼容层。

**已完成的修复**：
- ✅ 在代码中添加标志（wrangler.toml）
- ⏳ 需要在 Pages UI 中添加标志
