{
  "$schema": "https://pages.cloudflare.com/schema.json",
  "buildCommand": "npm run build && npm run pages:build",
  "buildOutputDirectory": ".vercel/output/static",
  "devCommand": "npm run dev",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  },
  "ignore": [
    ".git",
    ".github",
    "node_modules",
    ".wrangler"
  ]
}