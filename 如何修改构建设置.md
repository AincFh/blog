# 🔧 修改 Cloudflare Pages 构建设置

## 问题

您的项目当前配置:
- ❌ 构建命令: `npm run build`
- ❌ 输出目录: `.next`

需要改为:
- ✅ 构建命令: `npx @cloudflare/next-on-pages@1`
- ✅ 输出目录: `.vercel/output/static`

## 🎯 如何修改

### 在 Cloudflare Dashboard 找到构建设置

1. **进入项目**: Workers & Pages → blog-web
2. **点击 Settings**
3. **在左侧菜单寻找**:
   - "Builds & deployments" 或
   - "构建和部署" 或
   - "Build configuration"

### 如果找不到构建设置选项

这说明您的项目是通过 **Wrangler 手动部署**的，而不是通过 Git 自动部署。

**解决方案: 连接到 Git 仓库**

#### 步骤 1: 在 GitHub 创建仓库

1. 访问 https://github.com
2. 点击右上角 "New repository"
3. 创建一个新仓库（比如 `blog-web`）
4. **不要**初始化 README

#### 步骤 2: 在本地连接远程仓库

打开 PowerShell，运行:

```powershell
cd d:\Desktop\blog\web

# 添加远程仓库（替换为您的 GitHub 用户名）
git remote add origin https://github.com/您的用户名/blog-web.git

# 推送代码
git push -u origin master
```

#### 步骤 3: 在 Cloudflare Pages 连接仓库

1. 在 Cloudflare Pages 项目中
2. Settings → Builds & deployments
3. 点击 "Connect to Git"
4. 选择 GitHub，授权
5. 选择您刚创建的仓库
6. 配置构建命令:
   - Build command: `npx @cloudflare/next-on-pages@1`
   - Build output: `.vercel/output/static`

## 🚀 连接 Git 后的好处

- ✅ 每次 `git push` 自动部署
- ✅ 可以配置构建命令
- ✅ 有构建日志可以查看
- ✅ 支持预览部署（每个分支）

## 💡 临时测试方案

如果您想先测试，可以用标准构建:

1. 保持当前的 `npm run build` 和 `.next` 配置
2. 但这样 **API 路由可能不工作**
3. 只有静态页面会正常显示

## ❓ 需要帮助吗？

如果您：
- 不知道如何创建 GitHub 仓库
- 需要我帮您配置 Git
- 想了解更多细节

请告诉我！
